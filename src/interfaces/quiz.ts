export interface IQuiz {
    allowedEmails: string[];
    createdAt: string;
    description: string;
    id: string;
    status: string;
    tags: string[];
    title: string;
    updatedAt: string;
    uniqueToken: string;
}
